#include "Container.h"

int main() {
	Container tmp = Container();
	int ni = 5;
	tmp.GetDynamicViscosityField().SetDimensionSizeI(ni);
	tmp.GetDynamicViscosityField().SetDimensionSizeI(ni);
	tmp.GetDynamicViscosityField().BuildField();
	tmp.GetDynamicViscosityField().GetPoint(i, j).SetVar(x);
}